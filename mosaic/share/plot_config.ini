[global]
file_title = output.plot

[plot_1]
abscissae = nb_params
ordinates = test_loss, train_loss
include_keys = class, data_size
include_values = [dataset_OR, dataset_AND], [30, 60]
excludes = width, length, data_size
plot_type = line

[plot_2]
abscissae = dataset_key
ordinates = overfit
include_keys = class, data_size, class
include_values = [dataset_OR, dataset_AND], [30, 60], mlp_brick
excludes = 
plot_type = violin

